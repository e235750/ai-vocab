# 開発環境用Dockerfile
FROM node:20-alpine

WORKDIR /app

# package.json関連ファイルを先にコピー（キャッシュ効率化）
COPY package*.json ./

# 依存関係をインストール
RUN npm ci

# ホットリロード用のnodemon追加
RUN npm install -g nodemon

# 開発用ポートを公開
EXPOSE 3000

# 開発サーバーを起動（ホットリロード有効）
CMD ["npm", "run", "dev"]
